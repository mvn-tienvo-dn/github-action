name: Demo
on: 
  push:
    branches:
      - "main"
jobs:
  tien-le:
    runs-on: ubuntu-latest
    steps:
      - name: hello 
        run: echo abcbsdd
      - name: check source
        uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - run: npm i
      - run: npm run lint
